# Postman

Import `EnglishCenter.postman_collection.json`, đặt `baseUrl` đúng cổng API, sau đó chạy Login và Verify Login OTP. Postman tự lưu JWT vào biến `token` khi verify thành công. Cập nhật các biến ID trước khi chạy Create/Update/Delete.
